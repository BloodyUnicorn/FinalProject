<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hp.finalproject">

    <application
        android:name=".Fonts.FontBYekan"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".UserLogIn.LogIn"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity.MainActivity" />
        <activity android:name=".MainActivity.Fragments.FragmentNotes" />
        <activity android:name=".MainActivity.Fragments.FragmentHome" />
        <activity android:name=".MainActivity.Fragments.FragmentGallery"></activity>
    </application>

</manifest>